# Base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=flota_vehiculos

# Sesión — genera uno propio con: openssl rand -hex 32
SESSION_SECRET=cambia_esto_por_una_cadena_aleatoria_larga

# Servidor
PORT=3000
